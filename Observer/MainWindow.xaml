<Window x:Class="Observer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Title="Observer" Height="175" Width="500">
    <Border x:Name="LayoutRoot" Padding="5"
            BorderBrush="{StaticResource ApplicationForeground}" BorderThickness="2" 
            Background="{StaticResource ApplicationBackground}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button x:Name="ClickMeButton" Grid.Column="0"
                    Grid.RowSpan="4"
                    Content="Click Me!"
                    Style="{StaticResource GoButtonStyle}" />
            <TextBlock x:Name="TextBlock1" Grid.Column="1"
                       Grid.Row="1"
                       Style="{StaticResource ApplicationText}" />
            <TextBlock x:Name="TextBlock2" Grid.Column="1"
                       Grid.Row="2"
                       Style="{StaticResource ApplicationText}" />
        </Grid>
    </Border>
</Window>
